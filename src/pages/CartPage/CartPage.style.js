import styled from "styled-components";
import { motion } from "framer-motion";

export const TableTdImg = styled.img`
  width: 80px;
  height: 80px;
  object-fit: cover;
`;

export const MotionIcon = styled(motion.i)`
  cursor: pointer;
`;
